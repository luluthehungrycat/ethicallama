# ethllama systemd units are generated by `ethllama setup --service-mode system`.
# Do not install this file: generation resolves the invoking user, executable,
# and uses LoadCredential for /etc/ethllama/config.yaml.
