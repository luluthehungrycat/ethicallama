# Native extension — populated by maturin during wheel builds.
# During development (maturin develop), the module is at the
# top-level ethllama_core and imported by inference.py via
# _try_import_py_model() which tries both paths.
